<!--
Execution Contract — compressed handoff from planning artifacts to implementation.
Generated by reading proposal.md, specs/, design.md, tasks.md, plan.md.
This file is the single authority for what apply phase is allowed to do.
Do NOT edit manually after approval — if stale, regenerate.
-->

## Intent Lock

- **Change name**:
- **Problem to solve**:
- **In scope**:
- **Out of scope**:
- **Requirement count**:
- **Scenario count**:
- **Design decision count**:
- **Task batch count**:

## Approved Behavior

- **Approved requirement summary**:
- **Key scenarios**:
- **Acceptance checks**:

## Acceptance Anchors

<!--
  External requirement traceability. Filled at contract generation time
  by reading openspec/config.yaml's acceptance_doc.anchors.

  CRITICAL: this section references Anchor IDs ONLY. The verbatim
  acceptance statement lives in config.yaml (single source of truth).
  Do NOT copy the statement text here — copying invites silent softening.
  verify Check 9 joins this table's IDs back to config's statements.

  If openspec/config.yaml has no acceptance_doc declared (e.g. a library
  project with no external PRD), write "N/A — no external acceptance
  doc declared" and skip filling the table.
-->

- **Acceptance doc**: <from openspec/config.yaml: acceptance_doc.path>
- **Anchored items covered by this change**:

| Anchor ID | Maps to specs | Maps to tasks | Out of scope? (rationale if yes) |
|---|---|---|---|
| <ID from config> | <spec capability names> | <tasks §X.Y> | no / yes — <reason> |

<!--
  Only list anchors this change actually aims to cover. Anchors not
  listed here are "undeclared for this change" — verify Check 9 will
  surface a WARNING for each config-declared anchor absent from this
  table (the change may be intentionally not touching it, but the gap
  is made visible rather than silently ignored).
-->

## Design Constraints

- **Architecture constraints**:
- **Interface constraints**:
- **Dependency constraints**:
- **Data constraints**:

## Task Batches

### Batch 1

- **Goal**:
- **Completion criteria**:

## Test Obligations

- **Behaviors that must fail first**:
- **Required edge cases**:
- **Regression-sensitive areas**:

## Escalation Rules

- **When to rewind to planning (regenerate proposal/specs/design/tasks/plan)**:
- **When to rewind to bridging (regenerate this contract)**:
- **When implementation must not continue**:

## Approval

- **Approval time**:
- **User confirmation summary**:
